<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <!-- 영화 등록/조회/삭제 mapper -->
<mapper namespace="org.green.seenema.mapper.MovieCRUDMapper">
	
	<!-- 등록 -->
	<insert id="insertMovie">
		INSERT INTO seenema.movie_tbl(movieTitle, genre, plot, director, actors, viewAge, releaseDate, runningTime, postFileName, previewURL) 
			VALUES(#{movieTitle}, #{genre}, #{plot}, #{director}, #{actors}, #{viewAge}, #{releaseDate}, #{runningTime}, #{postFileName}, #{previewURL})
	</insert>
	
	<!-- 전체 조회 -->
	<select id="getList" resultType="org.green.seenema.vo.MovieVO">
		SELECT * FROM seenema.movie_tbl;
	</select>
	
	<!-- 영화 코드로 조회 -->
	<select id="selectOne" resultType="org.green.seenema.vo.MovieVO">
		SELECT * FROM seenema.movie_tbl WHERE movieCode = #{param1};
	</select>
	
	<!-- 영화 정보 수정 -->
	<update id="update">
		UPDATE seenema.movie_tbl SET movieTitle=#{movieTitle}, genre=#{genre}, plot=#{plot}, 
			director=#{director}, actors=#{actors}, viewAge=#{viewAge}, 
			releaseDate=#{releaseDate}, runningTime=#{runningTime}, 
			postFileName=#{postFileName}, previewURL=#{previewURL}
			WHERE movieCode=#{movieCode};
	</update>
	
	<!-- 영화 정보 삭제 -->
	<delete id="delete">
		DELETE FROM seenema.movie_tbl WHERE movieCode=#{param1};
	</delete>
</mapper>